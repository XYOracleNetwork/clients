import type { Payload } from '@xyo-network/payload-model'

export const sendTransaction = async (_elevatedPayloads: Payload[], _additionalPayloads: Payload[]): Promise<void> => {
  await Promise.resolve()
}
