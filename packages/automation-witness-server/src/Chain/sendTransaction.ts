import type { Payload } from '@xyo-network/payload-model'

export const sendTransaction = async (elevatedPayloads: Payload[], additionalPayloads: Payload[]): Promise<void> => {
  await Promise.resolve()
}
